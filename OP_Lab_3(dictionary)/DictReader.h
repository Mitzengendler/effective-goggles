#pragma once

#include <iostream>
#include <string>
#include <fstream>
#include <sstream>
#include <vector>
#include "HashTable.h"

void fillDict(HashTable x);
void printRes(HashTable x);
string toUpper(string word);